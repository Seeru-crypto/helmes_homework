export const links = [
    {
        href: "/",
        text: "Home"
    }
]

export const SecondsToMilliseconds = (seconds: number): number => seconds * 1000;

export const SlugSector = "sectors" as string
export const SlugUsers = "users" as string